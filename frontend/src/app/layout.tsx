import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "LifeOS AI — Personal Finance & Productivity Intelligence",
  description:
    "AI-powered personal operating system that analyzes expenses, income, spending patterns, productivity habits, and more. Features autonomous AI agents for financial analysis, anomaly detection, and personalized coaching.",
  keywords: [
    "AI",
    "finance",
    "productivity",
    "machine learning",
    "personal finance",
    "anomaly detection",
  ],
};

import { AuthProvider } from "@/contexts/auth-context";

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} dark h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  );
}
